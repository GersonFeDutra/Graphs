#include "../collections/list.h"

/* Grafo ponderado (com pesos não negativos) definido por lista de adjacências. */
typedef List *PGraphList[];

/* Grafo ponderado (com pesos não negativos) definido por matriz de adjacências. */
typedef float PGraphMatrix[][];
